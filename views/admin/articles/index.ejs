<%- include("../../partials/header.ejs") %>
<%- include("../../partials/navebar.ejs") %>
<body>
    <div class="container">
        <span class="d-flex justify-content-between">
            <h3 class="">Artigos</h3>
            <a href="/article/new" class="btn btn-primary ">ADD</a>
        </span>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>ID</th>
                <th>Título</th>
                <th>Categoria</th>
                <th>Incluído</th>
                <th>Ações</th>
            </tr>
            </thead>
            <tbody>
                <% Artigles.forEach(Artigle => { %>
                    <tr>
                        <td><%= Artigle.id %></td>
                        <td><%= Artigle.title %></td>
                        <td><%= Artigle.Category.title %></td>
                        <td><%= Artigle.createdAt %></td>
                        <td>
                            <a href="/article/editar/<%=Artigle.id%>" class="btn btn-info">EDITAR</a>
                            <form method="post" class="d-inline" action="/article/deletar" onsubmit="confirmarDelecao(event, this)">
                                <input type="hidden" name="id" value="<%= Artigle.id %>">
                                <button type="submit" class="btn btn-danger">-</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
<%- include("../../partials/footer.ejs") %>
